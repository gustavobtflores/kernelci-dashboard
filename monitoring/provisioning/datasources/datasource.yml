apiVersion: 1

datasources:
    - name: Prometheus
      type: prometheus
      access: proxy
      # Use the docker service name "prometheus" here
      url: http://prometheus:9090
      isDefault: true
      editable: true
